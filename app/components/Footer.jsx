import React from "react";
import styles from "../page.module.css"

const Header = ()=>{
    return(
        <footer className={styles.footer}>
           <p>Todos os direitos reservados a Rafael Rossetti</p>
        </footer>
    )}
export default Header;